<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>IK Analyzer 扩展配置</comment>
	<!--用户可以在这里配置自己的扩展字典 -->
	<entry key="ext_dict">custom/zhuanye_combine_save.txt</entry>
	<!--同义词扩展字典，但是其相对目录位于(relative to the ES_HOME/config location) -->
	<entry key="synonym_dict">analysis/synonym.txt</entry>
	<!-- <entry key="ext_dict">custom/mydict.dic;custom/single_word_low_freq.dic</entry>-->
	<!--用户可以在这里配置自己的扩展停止词字典-->
	<!-- <entry key="ext_stopwords">custom/ext_stopword.dic</entry>-->
	<!--用户可以在这里配置远程扩展字典 -->
	<!-- <entry key="remote_ext_dict">words_location</entry> -->
	<!--用户可以在这里配置远程扩展停止词字典-->
	<!-- <entry key="remote_ext_stopwords">words_location</entry> -->
	<!--用户可以在这里配置远程无效词字典：有些词想要在词库中去除，尤其是发现了一些 词库中的复合词-->
	<!-- <entry key="remote_remove_words">words_location</entry> -->
</properties>
